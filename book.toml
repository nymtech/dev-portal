[book]
authors = ["Oliver Anyanwu"]
language = "en"
multilingual = false
src = "src"

#################
# PREPROCESSORS #
################# 

# todo remove in place of preprocessor.theme 
[preprocessor.pagetoc]
additional-css = ["theme/pagetoc.css"]
additional-js  = ["theme/pagetoc.js"]

# NOTE for adding this in: delete the theme directory, uninstall mdbook-pagetoc, and then run mdbook build so it creates the theme directory again 
# [preprocessor.theme]
# pagetoc        = true
# # some variables related (defined in theme/css/variables.css)
# # `content-max-width` + `pagetoc-width` = 95% seems the best
# pagetoc-width            = "13%"
# content-max-width        = "82%"
# pagetoc-fontsize         = "14.5px"
# sidebar-width            = "300px"
# menu-bar-height          = "40px"   # memu-bar = the bar on the top
# page-padding             = "20px"
# mobile-content-max-width = "98%"

# # layout
# content-padding           = "0 10px"
# content-main-margin-left  = "10%"
# content-main-margin-right = "10%"
# nav-chapters-max-width    = "auto"
# nav-chapters-min-width    = "auto"
# chapter-line-height       = "2em"
# section-line-height       = "1.5em"

# # modify some fontsizes
# root-font-size    = "70%"    # control the main font-size
# body-font-size    = "1.5rem"
# code-font-size    = "0.9em"
# sidebar-font-size = "1em"    # sidebar = toc on the left

# # modify some colors under ayu/coal/light/navy/rust theme
# coal-inline-code-color  = "#ffb454"
# light-inline-code-color = "#F42C4C"
# navy-inline-code-color  = "#ffb454"
# rust-inline-code-color  = "#F42C4C"
# light-links             = "#1f1fff"
# rust-links              = "#1f1fff"

# # if true, never read and touch the files in theme dir
# turn-off = false

[preprocessor.admonish]
command = "mdbook-admonish"
assets_version = "2.0.0" # do not edit: managed by `mdbook-admonish install`

# variables preprocessor: import variables into files 
# https://gitlab.com/tglman/mdbook-variables/
[preprocessor.variables.variables]
# Code prerequisites versions
minimum_rust_version = "1.66"
# minimum_node_version = ""
# Nym binary versions 
platform_release_version = "v1.1.6"
# sdk_version = ""
# mixnet_contract_version = ""
# vesting_contract_version = ""
# wallet_version = ""
# nym_connect_version = "" 
# network_explorer_version = "" 

[preprocessor.last-changed]
command = "mdbook-last-changed"
renderer = ["html"]

# look into getting this to work properly... might be out of date? 
# [preprocessor.extended-markdown-table]

# more pre-processor plugins to look into from https://github.com/rust-lang/mdBook/wiki/Third-party-plugins & https://lib.rs/keywords/mdbook-preprocessor
# mdbook-mermaid - use mermaid to make diagrams from text!
# mdbook-i18n
# mdbook-theme - we might have to remove the existing pagetoc preprocessor as its bundled in it 

#########
# BUILD #
#########

[build]
build-dir = "book"                # the directory where the output is placed
create-missing = true             # whether or not to create missing pages
use-default-preprocessors = true  # use the default preprocessors
extra-watch-dirs = []             # directories to watch for triggering builds

##########
# OUTPUT #
########## 

[output.html]
default-theme = "coal"
preferred-dark-theme = "coal"
curly-quotes = true
# mathjax-support = false # useful if we want to pull equations in ? 
copy-fonts = true
no-section-label = false
additional-css = ["theme/pagetoc.css", "././mdbook-admonish.css", "last-changed.css"]
additional-js  = ["theme/pagetoc.js"]
git-repository-url = "https://github.com/nymtech/docs"
git-repository-icon = "fa-github"
# edit-url-template = "https://github.com/rust-lang/mdBook/edit/master/guide/{path}"
# site-url = "/example-book/"
# cname = "myproject.rs"
# input-404 = "not-found.md" # todo make custom 404 page

[output.html.fold]
enable = true    # whether or not to enable section folding
level = 0        # the depth to start folding

# controlling rust sample code blocks 
[output.html.playground]
editable = false         # allows editing the source code
copyable = true          # include the copy button for copying code snippets
copy-js = true           # includes the JavaScript for the code editor
line-numbers = false     # displays line numbers for editable code
runnable = true          # displays a run button for rust code

# options for the built in text search 
[output.html.search]
enable = true            # enables the search feature
limit-results = 30       # maximum number of search results
teaser-word-count = 30   # number of words used for a search result teaser
use-boolean-and = true   # multiple search terms must all match
boost-title = 2          # ranking boost factor for matches in headers
boost-hierarchy = 1      # ranking boost factor for matches in page names
boost-paragraph = 1      # ranking boost factor for matches in text
expand = true            # partial words will match longer terms
heading-split-level = 3  # link results to heading levels
copy-js = true           # include Javascript code for search

